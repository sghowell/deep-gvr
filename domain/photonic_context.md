# Photonic Context

- Focus area: photonic linear-optics computation and state handling using open-source tooling.
- Core concepts: modes, photons, basic states, interferometers, loss/noise models, processor configuration.
- Tier 2 implication: request photonic analysis when the claim depends on mode counts, photon counts, or processor/state compatibility that needs explicit tool-backed checking.
