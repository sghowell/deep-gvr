{
  "type": "object",
  "required": ["verdict", "tier1", "tier2", "tier3", "flaws", "caveats", "cannot_verify_reason"],
  "additionalProperties": false,
  "properties": {
    "verdict": { "type": "string", "enum": ["VERIFIED", "FLAWS_FOUND", "CANNOT_VERIFY"] },
    "tier1": {
      "type": "object",
      "required": ["checks", "overall", "flaws", "caveats"],
      "additionalProperties": false,
      "properties": {
        "checks": {
          "type": "array",
          "minItems": 1,
          "items": {
            "type": "object",
            "required": ["check", "status", "detail"],
            "additionalProperties": false,
            "properties": {
              "check": { "type": "string" },
              "status": { "type": "string", "enum": ["pass", "fail", "uncertain"] },
              "detail": { "type": "string" }
            }
          }
        },
        "overall": { "type": "string", "enum": ["VERIFIED", "FLAWS_FOUND", "CANNOT_VERIFY"] },
        "flaws": { "type": "array", "items": { "type": "string" } },
        "caveats": { "type": "array", "items": { "type": "string" } }
      }
    },
    "tier2": {
      "type": ["object", "null"],
      "required": ["simulation_requested", "reason", "simulation_spec", "results", "interpretation"],
      "additionalProperties": false,
      "properties": {
        "simulation_requested": { "type": "boolean" },
        "reason": { "type": "string" },
        "simulation_spec": { "type": ["object", "null"] },
        "results": { "type": ["object", "null"] },
        "interpretation": { "type": ["string", "null"] }
      }
    },
    "tier3": {
      "type": "array",
      "items": {
        "type": "object",
        "required": ["claim", "backend", "proof_status", "details", "lean_code"],
        "additionalProperties": false,
        "properties": {
          "claim": { "type": "string" },
          "backend": { "type": "string" },
          "proof_status": { "type": "string" },
          "details": { "type": "string" },
          "lean_code": { "type": "string" }
        }
      }
    },
    "flaws": { "type": "array", "items": { "type": "string" } },
    "caveats": { "type": "array", "items": { "type": "string" } },
    "cannot_verify_reason": { "type": ["string", "null"] }
  }
}
